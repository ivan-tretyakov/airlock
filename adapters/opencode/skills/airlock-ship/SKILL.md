---
name: airlock-ship
description: Airlock completion gate. Use when work is ready to verify, record in its ledger, and commit safely with evidence.
---

Read `../../../../skills/ship/SKILL.md` relative to this file and follow it as the canonical workflow. Its ledger template is beside that canonical `SKILL.md`.

Map canonical skill handoffs to OpenCode names: `brainstorm` → `airlock-brainstorm`, `plan` → `airlock-plan`, `ship` → `airlock-ship`, `review` → `airlock-review`, and `debug` → `airlock-debug`.
