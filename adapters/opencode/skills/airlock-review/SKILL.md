---
name: airlock-review
description: Airlock feedback workflow. Use for user, PR, CI, or reviewer feedback after a commit, or to resume shipped work from its ledger.
---

Read `../../../../skills/review/SKILL.md` relative to this file and follow it as the canonical workflow.

Map canonical skill handoffs to OpenCode names: `brainstorm` → `airlock-brainstorm`, `plan` → `airlock-plan`, `ship` → `airlock-ship`, `review` → `airlock-review`, and `debug` → `airlock-debug`.
