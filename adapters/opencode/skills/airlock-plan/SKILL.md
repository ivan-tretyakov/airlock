---
name: airlock-plan
description: Airlock delivery-pack planning and execution. Use after design or scope approval to define contiguous Crossings, host-agent/model routing, and required evidence gates before implementation.
---

Read `../../../../skills/plan/SKILL.md` relative to this file and follow it as the canonical workflow. In OpenCode, use `question` for per-pack split/routing/gate approval and `task` for subagents. The `task` tool selects a configured subagent, not an arbitrary model; map each portable work class and host role to existing configured agents, record the planned agent/model, and record the effective runtime mapping in gate or Crossing evidence.

Map canonical skill handoffs to OpenCode names: `brainstorm` → `airlock-brainstorm`, `plan` → `airlock-plan`, `ship` → `airlock-ship`, `review` → `airlock-review`, and `debug` → `airlock-debug`.
