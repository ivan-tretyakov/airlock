---
name: airlock-debug
description: Airlock systematic debugging. Use for unclear failures, regressions, wrong behavior, or fixes that did not hold.
---

Read `../../../../skills/debug/SKILL.md` relative to this file and follow it as the canonical workflow. In OpenCode, use `task` when an independent subagent is needed.

Map canonical skill handoffs to OpenCode names: `brainstorm` → `airlock-brainstorm`, `plan` → `airlock-plan`, `ship` → `airlock-ship`, `review` → `airlock-review`, and `debug` → `airlock-debug`.
