---
name: airlock-brainstorm
description: Airlock design and scope gate. Use before a feature, redesign, tool, non-trivial change, or any file-writing subagent when the approach or blast radius needs approval.
---

Read `../../../../skills/brainstorm/SKILL.md` relative to this file and follow it as the canonical workflow. In OpenCode, use `question` for structured questions and `task` for subagents.

Map canonical skill handoffs to OpenCode names: `brainstorm` → `airlock-brainstorm`, `plan` → `airlock-plan`, `ship` → `airlock-ship`, `review` → `airlock-review`, and `debug` → `airlock-debug`.
