process.stdout.write(
  "When Airlock Full work is active, reread the design, plan, the ledger's single Resume checkpoint plus only the sections it names, and docs/airlock/STATUS.md; never reread the whole ledger; otherwise ignore this compaction reminder.\n",
);
