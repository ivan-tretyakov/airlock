process.stdout.write(
  "Airlock compaction checkpoint: refresh docs/airlock/STATUS.md and the ledger Resume checkpoint; after compaction reread the design, plan, ledger checkpoint, and STATUS.md before dispatch.\n",
);
