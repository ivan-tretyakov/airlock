# Airlock 2.4 Review Resolution

**Date:** 2026-08-18
**Source review:** `2026-08-18-airlock-2.4-review.md`

## Resolved Before Release

- **R1:** `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` now declare version `2.4.0`.
- **R4:** validation failures after a manifest is parsed retain a safe, shape-checked `selectedRoute` in the blocked launcher summary. The summary never echoes unvalidated manifest fields; malformed or unsafe route values become `null`.
- **R5:** Windows-host validation completed after this correction: guard 39/39, plugin-policy 34/34, review-bundle 7/7, and external-runtime 96/96 all pass. Strict plugin and marketplace manifest validation also pass.

## Recorded Trade-Offs

- **R2:** Airlock 2.4 does not claim prompt-surface savings. The measured 2.3-to-2.4 command/reference surface increased by 7,550 bytes (11%). WS-1b's bounded ledger reads, not prompt slimming, are the expected context reduction. A separate future pass is required before claiming per-Crossing `ship.md` savings.
- **R3:** `orchestrator.md` intentionally reads the canonical sections in `commands/start.md`. This removes duplicated policy at the cost of one session-start indirection. Preserve this single-source-of-truth trade unless measurements show it is material; then load only the named start sections rather than restoring duplicated prose.

## Still Open After Merge

- The first post-merge weekly `ai-usage.py` reading must verify the measurement gate. The orchestrator model downgrade remains deferred until that reading.
- The dashboard-widgets ledger archive cleanup remains a separate one-time follow-up.
