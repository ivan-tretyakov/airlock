# Airlock 2.5 Review Resolution

**Date:** 2026-08-19
**Source review:** `2026-08-19-airlock-2.5-review.md`

## Resolved Before Release

- **R1:** `plugin-policy.test.mjs` enforces an 80,000-byte, LF-normalized ceiling over the core Full-flow prompt surface: `start`, `plan`, `ship`, `review`, `orchestrator`, and `LIFECYCLE`. The resolved surface is 78,486 bytes, leaving 1,514 bytes of headroom. The review's reported 78,400-byte baseline covers this set; all `commands/*.md` would be a different 95,493-byte set.
- **R2:** `commands/plan.md` removes the duplicated `Named criterion` and `Executor host role` columns. Complex/Critical criteria now live in `Why / criterion / independence`; `Execution reason` names a specialised verifier role when needed. The invalid-row and three-value `Executed by` rules remain unchanged.

## Post-Merge Checks

- **R3:** On the first Full deliverable, record whether a three-Crossing pack has at most one `independent` gate and whether an ordinary contained change routes Standard to `worker` or `code-standard` rather than `code-complex`.
- **R4:** In the first weekly `ai-usage.py` reading, record dispatches per pack, expensive-tier share, and weight-budget-exceedance PROGRESS-line count. Tighten the budget if expensive dispatches stay high while exceptions are frequent.

## Validation

- Windows: guard 39/39, plugin-policy 36/36, review bundle 7/7, and external runtime 96/96 pass.
- Strict plugin and marketplace manifest validation pass.
