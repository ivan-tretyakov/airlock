# Airlock 3.0 Second Review Resolution

**Date:** 2026-08-20
**Source review:** `2026-08-20-airlock-3.0-review-2.md`

## Resolved

| Finding | Resolution | Regression coverage |
|---|---|---|
| N1 | Airlock keeps a named recovery ref, then drops only its new `stash@{0}` entry; the user's existing stash stack remains unchanged. | `Airlock recovery preserves the user's stash stack` |
| N2 | Recovery preserves blocked-task and unclaimed paths only. Paths exclusively owned by another doing task stay in the worktree and audit output; ambiguous shared paths fail closed. | `blocking a parallel task leaves other lanes untouched`; `auditing one parallel task leaves foreign lane paths untouched` |
| N3 | An open `case: goal` decision stops selection, resumption, and start until answered. | `goal-level blocking decisions do not require a task` |
| N4 | Unborn repositories use index/worktree diffs instead of `HEAD`, producing normal Airlock boundary behavior. | `unborn repositories use normal Airlock boundary behavior` |
| N5 | `next` rejects unexpected positionals instead of silently discarding forwarded text. | `next rejects unrecognised positional input` |
| N6 | Plan-level decisions render `(blocks goal)`. | `goal-level blocking decisions do not require a task` |
| N7 | OpenCode reports the tagged GitHub global-install command when the `airlock` bin is unavailable. Package metadata now includes repository, files, and Node engine constraints. | package assertions in `prompt surface contains only the slim roles and two shims` |

## Remaining Release Checks

- Complete the same five-task plan on Claude Code and OpenCode before tagging 3.0.0.
