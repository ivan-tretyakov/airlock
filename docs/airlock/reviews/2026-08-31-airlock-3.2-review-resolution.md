# Airlock 3.2 Plan Authoring Guidance Review Resolution

**Date:** 2026-08-31
**Source review:** `2026-08-31-airlock-3.2-review.md`

## Resolved

| Finding | Resolution | Regression coverage |
|---|---|---|
| P1 | The branch is a documentation-only precursor, not the 3.2 release. Release manifests, the installation tag, and migration text intentionally remain at 3.1.0; the PR presents itself as authoring guidance for the existing release. A future release branch bumps all manifests together. | `npm pack --dry-run --json` reports `airlock@3.1.0` unchanged |
| P2 | The checker policy now states the exception directly in both documents: builder-run acceptance suffices for `light` and `standard` tasks whose acceptance one command captures; otherwise one consolidated checker is preferred; per-task checkers are reserved for `complex` and `critical`. The three rules no longer overlap. | Source inspection of `README.md` and `PROJECT-CONVENTIONS.template.md` |
| P3 | Guidance now reads "Default to `standard` when authoring a task." No schema default is implied; `validatePlan` continues to require an explicit risk on every task. | `npm test` schema invariants unchanged |

## Remaining Release Check

- None. Documentation-only change; `npm test` and strict plugin validation pass at the resolved commit.
