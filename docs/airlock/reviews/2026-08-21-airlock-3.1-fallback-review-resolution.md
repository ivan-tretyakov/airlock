# Airlock 3.1 Fallback Review Resolution

**Date:** 2026-08-21
**Source review:** `2026-08-21-airlock-3.1-fallback-review.md`

## Resolved

| Finding | Resolution | Regression coverage |
|---|---|---|
| H1 | Not reproduced at reviewed commit `a8f5b35`: the parent routing spec already cross-references the amendment and explicitly permits pre-result dispatch-error fallback at lines 27, 30, and acceptance 14. | Source inspection at `a8f5b35` |
| H2 | Successful `done` atomically moves the active local pin into router-state `completed`, keyed by task id, commit SHA, and host. The record retains the executing candidate and classified failures; tracked commits and plans remain model-neutral. | `done archives the executing fallback route by task and commit` |
| H3 | `fallback` requires a class from `auth`, `rate-limit`, `timeout`, `transport`, or `model-unavailable`; the class is retained in failure history. Configuration and runtime cap each task at two advances. This makes the orchestrator assertion auditable but does not claim to prove host behavior. | `fallback advances a clean doing task and protects every pinned candidate from pruning`; `fallback configuration fails closed on old versions, duplicates, and unknown variants` |
| H4 | `maxExpensive` is explicitly a declared `risk: critical` plan guard, not inferred provider cost. Unused route parameters were removed from the budget calculation. | `critical-risk budget parks critical work while allowing lower risks` |
| H5 | The 3.0 design now records the 3.1 decision to raise the coordinator-verb cap from seven to eight. Structured class guidance replaces longer trigger prose in both shims, recovering prompt headroom while retaining the 5,000-byte gate. | `prompt surface contains only the slim roles and two shims` |

## Remaining Release Check

- Drive one real plan end to end on Claude Code and OpenCode, including a deliberate classified fallback, before tagging 3.1.0.
