# Airlock 3.0 PR #6 Verification Resolution

**Date:** 2026-08-20
**Source verification:** `2026-08-20-airlock-3.0-pr6-verification.md`

## Completed Before Merge

| Item | Resolution | Verification |
|---|---|---|
| M2 | Ran the full suite on the Windows development host after the recovery-layer changes. | `npm test`: 26/26 passing |
| M3 | Ownership comparison folds path case on Windows and macOS while Git commands retain original path casing. | `owns folds case on case-insensitive hosts`; recovery, commit, and checker regressions pass on Windows |
| M4 | `airlock init --host opencode` installs missing project-local `.opencode/command/airlock.md` and six `.opencode/agent/airlock-*` bindings without overwriting an existing plan or binding. The package includes the command asset. | `OpenCode bootstrap installs missing command and bindings without replacing the plan`; `npm pack --dry-run` |
| M5 | Marked `scripts/airlock.mjs` executable in Git. | Git mode `100755` |

## Post-Merge Release Gate

Create and push `v3.0.0` immediately after PR #6 merges. The documented GitHub installation command resolves that tag. Then complete the same five-task plan on Claude Code and OpenCode before publication is announced.
