Preflight V3 capture — 2026-08-31 (corrected after full-capture re-check)
herdr 0.8.2, opencode 1.18.18, `opencode` resolved at /home/ivan-tretyakov/.local/bin/opencode

Question: does interactive OpenCode accept a launch-time agent selector
(`opencode --agent <name>` or equivalent)?

Method: the FULL `opencode --help` output was inspected for an `--agent`
option in its Options section. A truncated first capture (first 40 lines)
missed the tail of the Options block; the full capture below settles it.

Verdict: PRESENT on the interactive default command.
- `opencode --help` Options section includes `--agent  agent to use`
  (and `--variant`, `--prompt`, `--model`, `--auto`, ...).
  CORRECTION (2026-09-01): the parenthetical above wrongly lists `--variant`
  among the interactive options. The verbatim capture below is correct — the
  interactive Options section has NO `--variant`; `--variant` exists only on
  `opencode run` (headless). Superseded by the 1.18.25 capture in
  `opencode-cli-1.18.25.txt`; kept here with this note rather than silently
  deleted.
- `opencode run --help` includes the same `--agent` flag for headless runs.

Consequence per spec: dispatch Path A (native interactive agent) is legal on
OpenCode builds whose default command accepts `--agent`:
  herdr agent start <name> --kind opencode --pane <id> -- --agent <AGENT>
Path B (headless `opencode run` through `herdr pane run`) stays implemented
and is chosen automatically by preflight on builds where the flag is absent
(the flag list is version-dependent; preflight re-checks every run).

--- opencode --help (verbatim Options section, complete) ---
Options:
  -h, --help          show help                                                            [boolean]
  -v, --version       show version number                                                  [boolean]
      --print-logs    print logs to stderr                                                 [boolean]
      --log-level     log level                 [string] [choices: "DEBUG", "INFO", "WARN", "ERROR"]
      --pure          run without external plugins                                         [boolean]
      --port          port to listen on                                        [number] [default: 0]
      --hostname      hostname to listen on                          [string] [default: "127.0.0.1"]
      --mdns          enable mDNS service discovery (defaults hostname to 0.0.0.0)
                                                                          [boolean] [default: false]
      --mdns-domain   custom domain name for mDNS service (default: opencode.local)
                                                                [string] [default: "opencode.local"]
      --cors          additional domains to allow for CORS                     [array] [default: []]
  -m, --model         model to use in the format of provider/model                          [string]
  -c, --continue      continue the last session                                            [boolean]
  -s, --session       session id to continue                                                [string]
      --fork          fork the session when continuing (use with --continue or --session)  [boolean]
      --prompt        prompt to use                                                         [string]
      --agent         agent to use                                                          [string]
      --auto          auto-approve permissions that are not explicitly denied (dangerous!)
                                                                          [boolean] [default: false]
      --mini          start the minimal interactive interface             [boolean] [default: false]
      --no-replay     disable mini session history replay on resume and after resize       [boolean]
      --replay-limit  cap visible mini replay to the newest N messages                      [number]

--- opencode run --help (verbatim excerpt, relevant options) ---
      --agent        agent to use                                                           [string]
      --format       format: default (formatted) or json (raw JSON events)
                                          [string] [choices: "default", "json"] [default: "default"]
      --variant      model variant (provider-specific reasoning effort, e.g., high, max, minimal)
                                                                                            [string]
